[package]
name = "treasury_agent"
edition = "2024"

[dependencies]

# Two named addresses in one package: our app (treasury_agent) + the vendored
# Nautilus/Oyster enclave library (enclave). Both publish at the same package id.
[addresses]
treasury_agent = "0x0"
enclave = "0x0"
